# dev 环境同步配置
#   - VITE_SYNC_API = /api   → 走 vite dev proxy（vite.config.ts 已配）转发到 127.0.0.1:8787
#   - prod 环境由 .env.production 或构建期环境变量注入
#   - token 不再是构建期常量：每个用户登录后从服务端拿到，存 localStorage
VITE_SYNC_API=/api