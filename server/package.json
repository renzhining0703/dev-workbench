{
  "name": "dev-workbench-sync",
  "private": true,
  "type": "module",
  "version": "2.0.0",
  "engines": {
    "node": ">=22.5"
  },
  "scripts": {
    "start": "node --no-warnings index.mjs",
    "test": "node --test --no-warnings test/*.test.mjs",
    "test:smoke": "node --no-warnings test/smoke.mjs"
  },
  "dependencies": {
    "express": "^4.21.2",
    "express-rate-limit": "^7.4.1"
  }
}
